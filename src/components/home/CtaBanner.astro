<section class="cta-banner">
  <div class="container cta-banner__inner reveal">
    <h2 class="heading-3 cta-banner__title">Prête à vivre l'expérience Masoda ?</h2>
    <p class="cta-banner__text">Réservez votre place pour le prochain atelier et rejoignez la communauté.</p>
    <a href="https://www.billetweb.fr/paint-and-jam-with-love" target="_blank" rel="noopener" class="btn btn--primary cta-banner__btn">
      Réserver un atelier
    </a>
  </div>
</section>

<style>
  .cta-banner {
    background: var(--color-dark);
    padding-block: var(--space-16);
  }

  .cta-banner__inner {
    text-align: center;
    max-width: 600px;
  }

  .cta-banner__title {
    color: var(--color-text-on-dark);
    margin-bottom: var(--space-4);
  }

  .cta-banner__text {
    color: rgba(246, 239, 231, 0.7);
    font-size: var(--text-lg);
    margin-bottom: var(--space-8);
    line-height: var(--leading-normal);
  }

  .cta-banner__btn {
    padding: var(--space-4) var(--space-10);
    font-size: var(--text-base);
  }
</style>
